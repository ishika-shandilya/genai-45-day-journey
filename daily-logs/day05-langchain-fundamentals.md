# Day 5 — LangChain Fundamentals (June 27, 2026)

## ✅ Topics covered
- LangChain pipe operator — chaining components cleanly
- ChatPromptTemplate — structuring prompts professionally
- Output parsers — StrOutputParser for clean responses
- RunnableSequence — sequential chain execution
- RunnableParallel — parallel chain execution
- RunnablePassthrough — passing input unchanged
- RunnableLambda — wrapping Python functions into chains

## 💡 Key insight
LangChain pipe operator mirrors analytical frameworks
from Economics — raw input flows through structured
stages to produce clean output. Same logic, new domain.
